import React from "react";

const adminLayout = {
    admin: {
        navBar: ['Preview vacations', 'Statistics', 'Vacation management'],
    },
    customer: {
        navBar: ['Browse vacations', 'Logout']
    },
    geust: {
        navBar: ['Browse vacations', 'Login', 'Register']
    }
}

export default adminLayout;
